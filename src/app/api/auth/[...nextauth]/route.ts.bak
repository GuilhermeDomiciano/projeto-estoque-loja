// Placeholder para evitar erros de build

// altere o nome do arquivo para route.ts ao inves de route.ts.bak pra ele voltar a funcionar
// fiz isso so pra nao quebrar o vercel 
export async function GET(request: Request) {
  return new Response("Endpoint em construção", { status: 200 });
}
  
export async function POST(request: Request) {
  return new Response("Endpoint em construção", { status: 200 });
}